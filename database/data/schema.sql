DROP DATABASE IF EXISTS soundClout;

CREATE DATABASE soundClout;

USE soundClout;

-- CREATE TABLE soundClout (
--   id INT PRIMARY KEY AUTO_INCREMENT,
--   title VARCHAR(40),
--   watched TINYINT NOT NULL DEFAULT 0,
--   UNIQUE KEY (title)
-- );