const db = require('../index');

module.exports = {
  songGenerator: () => {
    // TBD: write code to generate a single song instance
  },
  previousPlays: () => {
    // TBD: write code to generate a single previous play instance
  },
  upNext: () => {
    // TBD: write code to generate a single upNext play instance
  }
};